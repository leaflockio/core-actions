name: Pre-release

on:
  push:
    branches: [pre-main]

jobs:
  release:
    uses: ./.github/workflows/tpl-release.yml
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
